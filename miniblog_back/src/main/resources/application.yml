server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  jackson:
    date-format: yyyy-MM-dd hh:mm:ss
    time-zone: GMT+8
  http:
    encoding:
      force: true
      charset: UTF-8
      enabled: true

